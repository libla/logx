SET(NAME gtest)
FILE(GLOB_RECURSE FILELIST *.cpp *.cxx *.hpp *.c *.h ${CMAKE_CURRENT_SOURCE_DIR}/../include/${NAME}/*)
LIST(LENGTH FILELIST LENGTH)
IF(LENGTH GREATER 0)
	ADD_LIBRARY(${NAME} STATIC EXCLUDE_FROM_ALL ${FILELIST})
ENDIF()